# Large data files
*.csv
__pycache__/
*.pyc
*.pyo

# Build artifacts (C++)
c++engine/build/
*.o
*.obj
*.exe

# But keep compiled WASM (needed to run the app)
# hexuki.js and hexuki.wasm are intentionally tracked

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Temporary files
*.tmp
*.bak

# Node modules (if any)
node_modules/
nul

# Benchmark build output + transient runs (keep fixtures.json, baseline.json, scripts)
bench/engine/
bench/engine-*/
bench/runs/
c++engine/wasm_node/

# Native toolchain (portable MinGW) — local, do not commit (262MB)
tools/
native/
